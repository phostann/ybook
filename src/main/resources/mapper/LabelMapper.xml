<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.ybook.mapper.LabelMapper">

    <!-- 标签实体结果映射 -->
    <resultMap id="labelResultMap" type="com.example.ybook.entity.LabelEntity">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="use_count" property="useCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
    </resultMap>

    <!-- 基础查询SQL片段 -->
    <sql id="baseColumnList">
        id, name, use_count, create_time, update_time
    </sql>

    <!-- 根据标签名称列表批量查询 -->
    <select id="selectByNames" resultMap="labelResultMap">
        SELECT <include refid="baseColumnList"/>
        FROM y_label
        WHERE name IN
        <foreach collection="names" item="name" open="(" separator="," close=")">
            #{name}
        </foreach>
    </select>

</mapper>